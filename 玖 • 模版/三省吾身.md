<%*
// ===================== 全局变量与动态日期时间 =====================
const weekdays = ["星期日","星期一","星期二","星期三","星期四","星期五","星期六"];
const todayStr = tp.date.now("M月D日");
const weekdayStr = weekdays[Number(tp.date.now("d"))];
const currentTimeShort = tp.date.now("HH:mm"); // 当前时间，可选使用

// ===================== 用户输入：盘面数据 =====================
const title = await tp.system.prompt("今日标题后缀（留空则不显示）", "A股叒好了？");
const indexChange = await tp.system.prompt("指数涨均（% 如 +1.87）", "+ 1.87");
const keChuangChange = await tp.system.prompt("科创狂欢（% 如 +4.78）", "+ 4.78");
const volChange = await tp.system.prompt("成交量较昨日变化（亿元 如 +4460）", "+ 4460");

// ===================== 用户输入：青睐板块 =====================
const defaultSectors = [
  { name: "先进封装", display: "+ 155.26", value: "155.26", trigger: "13:13" },
  { name: "有色金属概念", display: "一骑绝尘", value: "141.87", trigger: "09:38-11:13 全部触发" },
  { name: "专精特新", display: "+ 114.04", value: "114.04", trigger: "13:18" },
  { name: "英伟达概念", display: "+ 107.73", value: "107.73", trigger: "09:58" },
  { name: "OLED", display: "+ 100.96", value: "100.96", trigger: "09:54" }
];
const numSectorsInput = await tp.system.prompt("输入板块数量", String(defaultSectors.length));
const numSectors = parseInt(numSectorsInput) || defaultSectors.length;
const sectors = [];
for (let i = 0; i < numSectors; i++) {
  const def = defaultSectors[i] || { name: "", display: "", value: "", trigger: "" };
  const name = await tp.system.prompt(`板块${i+1}名称（留空跳过）`, def.name);
  if (!name) continue;
  const display = await tp.system.prompt(`「${name}」列表显示内容`, def.display);
  const value = await tp.system.prompt(`「${name}」资金数值（亿元）`, def.value);
  const trigger = await tp.system.prompt(`「${name}」触发时间`, def.trigger);
  sectors.push({ name, display, value, trigger });
}

// ===================== 用户输入：高亮板块与总结 =====================
const highlightName = await tp.system.prompt("高亮板块名称（留空跳过）", "金属概念");
let highlightValue = "";
if (highlightName) {
  const found = sectors.find(s => s.name.includes(highlightName) || highlightName.includes(s.name));
  highlightValue = found ? found.value : await tp.system.prompt("高亮板块资金数值（亿元）", "141.87");
}
const summaryText = await tp.system.prompt("盘中总结/提示（留空则不输出）", "所以资金本身去科技部分的意愿就偏低，谁都知道科技板块还没企稳。");
%>
---
tags:
  - 持仓
最后更新: <% tp.date.now("YYYY-MM-DDTHH:mm:ss") %>
Including:
  - name
  - id
  - open
  - close
  - high
  - low
  - volumn
---

<%* // ===================== 标题区 ===================== %>
> [!tips] **<% todayStr %> <% weekdayStr %> <% title %>**
> - **今日盘面：**
>    - 指数涨均**<font color="#c00000"><%= indexChange %>%</font>**
>    - 科创狂欢**<font color="#c00000"><%= keChuangChange %>%</font>**
<% if (volChange) { %>
>    - 成交量较昨日 **<font color="#c00000"><%= volChange %></font>** 亿
<% } %>
<% if (sectors.length) { %>
> - **青睐板块：**
<% for (const s of sectors) { 
     const color = s.display.includes("-") ? "#00b050" : "#c00000";
%>
>    - <%= s.name %>：**<font color="<%= color %>"><%= s.display %></font>**<% if (s.trigger) { %> - 触发时间：**<font color="#00b050"><%= s.trigger %></font>**<% } %>
<% } %>
<% } %>
 
<%* // ===================== 高亮提示（可选） ===================== %>
<% if (sectors.length > 0 && highlightName && highlightValue) { %>
> [!caution]+ →  “<%= highlightName %>闷声干大事？” **<font color="#c00000"><%= highlightValue %></font>** 亿
>```chart
> type: bar
> labels: [<%= sectors.map(s => s.name).join(", ") %>]
> series:
>   - title: 资金量
>     data: [<%= sectors.map(s => s.value).join(", ") %>]
> tension: 1
> width: 100%
> labelColors: true
> fill: true
> beginAtZero: true
> bestFit: false
> bestFitTitle: undefined
> bestFitNumber: 0
>```
<% } %>

<%* // ===================== 正文段落 ===================== %>
<% if (summaryText) { %>
<%= summaryText %>
<% } %>

<%* // ===================== 观察表格 ===================== %>
| name | id | open | close | high | low | vol |
| ---- | -- | ---- | ----- | ---- | --- | --- |
|      |    |      |       |      |     |     |
|      |    |      |       |      |     |     |
|      |    |      |       |      |     |     |
|      |    |      |       |      |     |     |
|      |    |      |       |      |     |     |
|      |    |      |       |      |     |     |
|      |    |      |       |      |     |     |