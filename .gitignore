# local sync script (contains repo path, do not push)
sync.sh

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~
.idea/
.vscode/

# logs / temp
*.log
*.tmp
*.bak
.cache/

# credentials (never push)
.git-credentials

# agent working dir (automations, memory, skills config - never push)
.codebuddy/

# obsidian runtime caches (binary, regenerable)
.vault-intelligence/
.dashboard-backup/
TaskNotes/

# obsidian local config (per-device, never push — would override other machines' settings)
.obsidian/
