# 2026-09-13

## 知识库系统化审阅 + 改进方案
- 审阅了 `/home/AI/笔记/知识库`（2775 md / 652 pdf / ~3300+ 文件）。
- 关键发现：Frontmatter 98.2%、Wikilink 96.3% 采用；但两套 schema 并存（研报 schema vs 产业概念 schema）；2536 篇研报平铺于 `肆 • 机构观点` 根（无每行业 MOC）；`status` 状态机基本未用；断链 234 个（含生成器尾斜杠 bug `概念卡片/周期资源品\`、跨库残留 `../01-Macro/...`、占位符 `xxx`）；全库仅 3 canvas+1 drawio+15 图表块，产业链可视化稀疏；根目录 3 份重叠 dashboard 配置；框架页"15 张概念卡片"与现实(14)漂移。
- 产出改进方案：`零 • 导览/知识库审阅与改进方案.md`。核心：在现有双 schema+三层框架(00-研报知识框架 Raw→Auto-index→Wiki)上叠加 industry-wiki 可生长机制——统一 frontmatter(加 status/confidence/related/open_questions)、每行业一张 MOC + 一张产业链 canvas、研报按 concept 分桶解平铺、降低 Dataview 硬编码依赖、修断链、适配孤儿/缺口校验脚本。分 P0–P4 落地。
- 下一步待用户确认：P0 断链修复 / 标准件三模板 / 样板行业 / 适配校验脚本。

## 改进方案执行（Agent 模式）
- 用户确认执行；重点诉求：研报 frontmatter 已含 org(机构)/object(研究标的)/declareDate(发布日期)，无需迁移；文件以**报告标题**命名。
- 改名：**2288 篇** `研报_YYYYMMDD_NNN.md` → 标题（含剥离采集器包裹的 `_标题_`）；**241 篇**无真实 title（采集器回退名）保留代码名不强行改。
- 全库 `[[研报_X]]`→`[[标题]]` 链接重写（17 文件+时间线）。`.canvas` 内指向改名研报的挂链 = 0。
- 采集器防回归：`fetch_reports.py` 的 `note_id()` 改为"文件名=标题"+消歧；`_gen_obsidian.py` glob 改为遍历全部 .md 且要求 frontmatter 含"研报"，重建索引不丢改名研报。
- 模板三件套落地：`肆 • 机构观点/90-标准件/`（`00-README`/`研报笔记模板`/`行业概念卡片模板`/`行业MOC模板`/`产业链Canvas模板(.md+.canvas)`）。
- 验证（路径感知解析）：改名引入断链 0；尾斜杠 bug 已修 3 文件/18 链接；畸形 `[[研报__001]]` 中和 3 处；全库真实断链 500（均既有，非改名引入：文件夹链接/缺失概念卡/canvas 路径错位）。
- 回滚点：`/home/AI/.wk_backup/kb_md_20260913/` + `report_rename_mapping.json`/`cleanup_mapping.json`。
- 残余（未做，待确认）：500 既有断链治理、241 篇补真实 title、P2 解平铺/P3 每行业 canvas/P4 校验脚本。
